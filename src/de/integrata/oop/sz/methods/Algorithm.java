package de.integrata.oop.sz.methods;

public class Algorithm {

	public void printOrderConfirmation() {
		printAddress();
		printSalutation();
		printShoppingBasket();
		printRebate();
		printTotal();
	}
	
	public int calculateRebate() {
		int result
		if (customer.isVip())
			return 10;
		if (orderValue > 100) {
			result = 5
		}
		if (basket.containsSpecialItems()) 
			result += basket.specialRebate();
			
					
			
			
		
		
		
	}
	
}
